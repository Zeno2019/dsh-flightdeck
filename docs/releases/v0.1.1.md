# DSH Flightdeck v0.1.1

## English

**Full changelog:** Maintenance release — derives the bundled DSH version and the splash page version at runtime instead of hardcoding them.

## 中文

**完整变更日志:** 维护版 —— 内置 DSH 版本与 splash 页版本改为运行时推导,不再硬编码。